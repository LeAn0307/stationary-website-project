spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: product-service
  profiles:
    active: hau

server:
  port: 8088
# dang ky voi eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

#logging:
#  level:
#    root: DEBUG





















